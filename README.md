# Towards Efficient Matching of Regexes with Backreferences using Register Set Automata

This is an artifact reproducing the experimental results of the above paper. 

### Instalation

The following commands install and compile tools and packages neccessary for the experiments and their evaluation:

```shell
cd rsaregex-artifact
./install.sh
```

Requires sudo password to be filled in.

### Running experiments

The experiments can be ran using the following command:

```shell
cd experiments
./run-measurements.sh
```

This takes about 10 hours, we suggest running with nohup.

```shell
nohup ./run-measurements.sh &
```

The evaluation is generated by the following commands:

```shell
cd results
./create-csvs.sh
cd ..
./eval.py
```

This prints a textual table as in the paper, and generates pdf scatterplots comparing the tool to all the other tools.
